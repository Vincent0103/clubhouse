<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/css/styles.css" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Montserrat:ital,wght@0,100..900;1,100..900&family=Mozilla+Headline:wght@200..700&display=swap"
      rel="stylesheet"
    />
    <title><%= statusCode %> Error | The Clubhouse</title>
  </head>
  <body class="font-montserrat bg-rose-50">
    <nav class="fixed top-0 z-20 w-full border-b-1 border-rose-200 bg-white">
      <%- include("partials/nav.ejs") %>
    </nav>
    <main class="top-[69px] flex h-[600px] items-center justify-center">
      <div class="relative text-center">
        <!-- <div class="absolute -top-10 left-20 mb-8">
          <i
            class="mb-4 size-16 text-rose-500"
            data-lucide="alert-triangle"
          ></i>
        </div> -->
        <h1 class="mb-4 text-8xl font-bold text-rose-900"><%= statusCode %></h1>
        <h2 class="mb-4 text-3xl font-semibold text-gray-800">
          <% if (statusCode === 404) { %> Page Not Found <% } else if
          (statusCode === 403) { %> Access Forbidden <% } else if (statusCode
          === 500) { %> Internal Server Error <% } else { %> Something Went
          Wrong <% } %> &#58;&#40;
        </h2>
        <p class="mx-auto mb-8 max-w-md text-xl text-gray-600">
          <%= message || 'An unexpected error occurred.' %>
        </p>
        <div class="flex justify-center gap-4">
          <a
            href="/"
            class="mt-2 cursor-pointer rounded-md bg-rose-950 px-4 py-1.5 font-medium text-white transition-colors hover:bg-rose-800"
          >
            Go Home
          </a>
          <a
            onclick="history.back()"
            class="mt-2 cursor-pointer rounded-md bg-zinc-100 px-4 py-1.5 shadow-xs transition-colors hover:bg-zinc-200"
          >
            Go Back
          </a>
        </div>
      </div>
    </main>
    <script src="https://unpkg.com/lucide@latest"></script>
    <script>
      lucide.createIcons();
    </script>
  </body>
</html>
